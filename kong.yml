_format_version: "3.0"

services:
  - name: example-service
    url: http://httpbin:8080
    routes:
      - name: example-route
        paths: ["/anything"]
    plugins:
      - name: rate-limiting
        config:
          policy: local
          limit_by: ip
          minute: 5
          hour: 100
